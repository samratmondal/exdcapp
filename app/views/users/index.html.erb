<% provide(:title, 'All Users') %>


<div class="container">
  <div class="page-header">
    <h1>All Users</h1>
  </div>

  <div class='row'>
  	<div class="span8 offset2">
      <%= will_paginate %>

	    <p>
	      <%= link_to "Add User", add_user_path, class: "btn btn-primary" %>
	    </p>
	    <p>
	    	<h5>Click on User Name link to edit or delete a user.</h5>
	    </p>
   
	    <table id="userTable" class="table table-striped table-bordered tablesorter">
	      <thead>
		      <tr>
		        <th>User Name</th>
		        <th>User ID</th>
		        <th>First Name</th>
		        <th>Last Name</th>
		        <th>Admin Flag</th>
		        <th>Read Only</th>
		      </tr>
	      </thead>
	      <tbody>
          <% @users.each do |user| %>
		      <tr>
		        <td><%= link_to user.user_name, user %></td>
		        <td><%= user.cmmi_user_id %></td>
		        <td><%= user.first_name %></td>
		        <td><%= user.last_name %></td>
		        <td><%= user.admin_flag %></td>
		        <td><%= user.read_only %></td>
		      </tr>
		      <% end %>
		    </tbody>
	    </table>

      <%= will_paginate %>

	  </div>
	</div>
	<br />
</div>

